# HybridMind - Vector + Graph Native Database

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
gunicorn>=22.0.0
pydantic>=2.7.0
pydantic-settings>=2.0.0

# Vector Search
# GPU acceleration for embeddings works via PyTorch CUDA.
# faiss-gpu (Linux/Docker only — no Windows wheel):
#   pip install faiss-gpu>=1.7.4  # then set HYBRIDMIND_USE_FAISS_GPU=true
faiss-cpu>=1.7.4
numpy>=1.24.0

# Embeddings
sentence-transformers>=2.2.2
# CrossEncoder reranker (default: mixedbread-ai/mxbai-rerank-large-v2, see config.reranker_model)
# and bge-m3 local embedding fallback:
FlagEmbedding>=1.2.10
# PyTorch with CUDA support - install separately with:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Optional research modules (off by default):
# spaCy for entity co-occurrence edges:    pip install spacy && python -m spacy download en_core_web_sm
# GNN reranker:                            pip install torch-geometric

# Graph
networkx>=3.2.1

# Storage
aiosqlite>=0.19.0
nltk>=3.8.1

# Sparse Retrieval (fast BM25 + optional learned sparse)
# bm25s: 100x faster than pure-Python BM25, mmap-backed, NumPy-accelerated
bm25s>=0.2.0
PyStemmer>=2.2.0       # Snowball stemmer C extension used by bm25s
# fastembed: SPLADE-v3 learned sparse retrieval (optional, ~500MB model)
# pip install fastembed   # or: pip install "fastembed[gpu]"

# Caching & Rate Limiting
cachetools>=5.3.0
psutil>=5.9.0

# UI
streamlit>=1.28.0
plotly>=5.18.0
requests>=2.31.0
pandas>=2.0.0

# Utilities
python-multipart>=0.0.6
orjson>=3.9.10

# LLM Integration (OSM API)
openai>=1.0.0

# CLI
typer>=0.9.0
rich>=13.7.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
httpx>=0.25.1

# No benchmarks needed.
hypothesis
mcp[cli]>=1.0.0
